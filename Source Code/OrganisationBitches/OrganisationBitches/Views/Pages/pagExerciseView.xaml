<Page x:Class="OrganisationBitches.Views.Pages.pagExerciseView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
	Title="Exercise Planner" Loaded="Page_Loaded">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal">
            <Button Name="btnSave" Click="btnSave_Click">Save</Button>
            <Button Name="btnCurrentChart" Click="btnCurrentChart_Click">Current Chart</Button>
            <DatePicker Name="dpChartDate" VerticalAlignment="Center" SelectedDateChanged="dpChartDate_SelectedDateChanged"></DatePicker>
        </StackPanel>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <DataGrid Name="dgExerciseLog">
                
            </DataGrid>
        </ScrollViewer>
    </Grid>
</Page>
